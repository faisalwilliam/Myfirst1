
// Köra huvud bank programslingan
/*void run()
{
	while (true)
	{
		handlelogin(); // Hantera användaringlogginng
		bool isRunning = true;
		// När användare är inloggad
		while (isRunning)
		{
			displayMainMenu(); // Vissa huvudmeny
			int choice;
			cin >> choice;
			// Utför vald åtgärd
			switch (choice)
			{case 1:
				viewAccounts();
				break;
			case 2:
				transferFunds();
				break;
			case 3:
					exchangeCurrency();
					break;
				case 4:
					logout();
					isRunning = false;
					break;
                default:
					cout << "\tOgiltigt val försök igen. \n";
			}

		}

	*/
